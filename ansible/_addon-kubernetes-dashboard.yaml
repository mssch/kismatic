---
  - hosts: master[0]
    name: "{{step | default()}}Configure Kubernetes Dashboard"
    remote_user: root
    become_method: sudo
    run_once: true

    roles:
      - addon-kubernetes-dashboard
