---
  - hosts: master
    name: "Install Kubernetes Scheduler"
    remote_user: root
    become_method: sudo
    vars_files:
      - group_vars/all.yaml

    roles:
      - kubeconfig
      - scheduler
