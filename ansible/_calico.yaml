---
  - hosts: master:worker
    name: "{{step | default()}}Configure Cluster Network"
    remote_user: root
    become_method: sudo
    vars_files:
      - group_vars/all.yaml

    roles:
      - kubeconfig
      - network-environment
      - calico
